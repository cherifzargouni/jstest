const path = require('path');

module.exports = path.dirname(process.mainModule.filename);
// làol
//git tag version0.1 -m"version sans mise en page"


git tag version0.1 -m"version sans mise en page"